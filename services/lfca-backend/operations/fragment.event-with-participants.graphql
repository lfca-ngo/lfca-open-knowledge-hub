fragment EventWithParticipants on Event {
  ...Event
  participants(filter: { status: USER_RSVP_ACCEPTED }) {
    id
    isExternal
    user {
      ... on User {
        company {
          id
          logoUrl
        }
        id
      }
    }
  }
}
