mutation createActionComment($input: CreateActionCommentInput!) {
  createActionComment(input: $input) {
    attachments {
      fileName
      fileSize
      id
      mimeType
      source
    }
    createdAt
    id
    message
  }
}
